# Cloudflare Pages — URL rewrites.
#
# Intentionally empty. All host/SPA routing is handled in
# functions/_middleware.js (the Connect/admin subdomain split + the
# ops-console SPA shell served at the admin host root). _redirects
# can't do host-conditional rules, which is why this lives in the
# middleware instead.
